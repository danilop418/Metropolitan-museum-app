<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    android:background="@drawable/work_of_art_card_background">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/workOfArt"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:contentDescription="@string/workOfArt_image_desc" />

        <ImageButton
            android:id="@+id/favoriteButton"
            android:layout_width="@dimen/favorite_btn_size"
            android:layout_height="@dimen/favorite_btn_size"
            android:layout_gravity="top|end"
            android:layout_margin="@dimen/favorite_btn_margin"
            android:background="@drawable/favorite_button_background"
            android:src="@drawable/ic_favorite_border"
            android:contentDescription="@string/favorite_button_desc" />

        <TextView
            android:id="@+id/workOfArName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:padding="@dimen/spacing_small"
            android:text="Work Of Art name"
            android:textSize="@dimen/text_size_medium"
            android:textColor="@color/electric_gold"
            android:textStyle="bold"
            android:gravity="center"
            android:fontFamily="sans-serif-black" />
    </FrameLayout>
</com.google.android.material.card.MaterialCardView>